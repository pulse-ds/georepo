# Data files (2.3 GB of GeoJSON) ARE tracked in git (round 257):
# files >100 MB go through Git LFS (see .gitattributes).
# See DEV.md "Version control policy".

# Python venv (recreate: python3 -m venv .tools/venv && .tools/venv/bin/pip install pyshp)
.tools/venv/

# keep the catalog source of truth
!.tools/catalog.json

# Download/conversion cache
.tools/cache/

# Python bytecode
__pycache__/
*.pyc
